Remove local keyword from non-function declarations
telescope
get more familiar using dpkg as my package manager for installed packages, and pacman + yay + flatpak as installers + updaters. Try using dpkg for the installed packages instead of packman (will have to update both import and export).
Define list of package installers/updates per system in terms of preference i.e. on arch pacman > flatpak > yay
are there any settings for pacman, apt, yay and flatpak to only download latest packages if they're a week old? (to avoid supply chain attacks?) (If the latest is under a week, then grab the previous version, if it's over a week, etc.) (Claude says no, can add it via pr?)

Better startup behaviour
Don't stow top level files
Doesn't have zinit installed or zkbd setup or oh my posh or zoxide
Add signing key back to git config (Keep in mind that user.signingkey might have to be platform dependant)



xterm-256-color-:0 is (should be generated by script (using zkbd too)):
typeset -g -A escaped_modifier
escaped_modifier[Ctrl]="5"
escaped_modifier[Shift]="2"
escaped_modifier[Alt]="3"

typeset -g -A key
key[F1]='^[OP'
key[F2]='^[OQ'
key[F3]='^[OR'
key[F4]='^[OS'
key[F5]='^[[15~'
key[F6]='^[[17~'
key[F7]='^[[18~'
key[F8]='^[[19~'
key[F9]='^[[20~'
key[F10]='^[[21~'
key[F12]='^[[24~'
key[Insert]='^[[2~'
key[Home]='^[[H'
key[PageUp]='^[[5~'
key[Delete]='^[[3~'
key[End]='^[[F'
key[PageDown]='^[[6~'
key[Up]='^[[A'
key[Left]='^[[D'
key[Down]='^[[B'
key[Right]='^[[C'

key[Backspace]='^?'
key[Ctrl+Backspace]="^H"
key[Shift+Backspace]="^?"
key[Alt+Backspace]="^[^?"